#include <iostream>
#include <map>
#include <vector>
#include <limits>
#include <math.h>
using namespace std;
enum class TaskStatus
{
    NEW,         // новая
    IN_PROGRESS, // в разработке
    TESTING,     // на тестировании
    DONE         // завершена
};
int main()
{
    map<TaskStatus, int> tasks;
    task[TaskStatus::NEW + 1] = 5;
}